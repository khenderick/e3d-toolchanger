; Toolhead
echo sensors.gpIn[1].value = 0 ? "No tool" : "Tool", "detected on Toolhead"
echo "Tool 0 is", sensors.gpIn[3].value = 0 ? "not docked" : "docked"
echo "Tool 1 is", sensors.gpIn[4].value = 0 ? "not docked" : "docked"
